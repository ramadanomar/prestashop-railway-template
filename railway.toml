[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# PrestaShop first install (DB schema + demo fixtures) can take 3-5 minutes
# Set a generous timeout to avoid false deployment failures
healthcheckPath = "/"
healthcheckTimeout = 600
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
